---
summary: "家庭保单健诊智能体：拍照识别全家保单（识别失败自动转手动录入）→按被保险人归属到家庭成员→结合家庭结构执行成员级+家庭级6维度诊断→家庭保障矩阵→首页执行摘要（健康总分+TOP3风险）→三权分立确权传承分析→风险高亮→自动排版打印11模块家庭健诊报告"
read_when:
  - 用户上传保单照片需要识别整理（含全家多人保单）
  - 用户提到"保单健诊""保单诊断""保单体检""保障分析""家庭保单"
  - 用户需要结合家庭结构分析保障缺口
  - 用户需要生成家庭保单诊断报告
  - 用户提到"加保建议""保障缺口""确权""配偶保障""子女保障"
  - 用户需要打印保单分析报告
  - 用户提到"资产配置""家庭风险""税务规划""保单贷款"
  - 照片识别失败需要手动录入保单信息，或用户主动选择"手动填"
---

# 家庭保单健诊智能体 Skill

## 核心场景

银行渠道理财经理/客户经理为客户做**家庭保单健诊**服务：员工拍摄客户全家的保单照片（投保人可能不同、被保险人覆盖夫妻/子女/老人），智能体自动识别多张保单信息、**按被保险人归属到家庭成员**、结合家庭结构（成员构成、各自收入、负债、子女老人情况）执行成员级+家庭级诊断分析、高亮风险缺口、自动排版生成可打印的家庭健诊报告，辅助客户经理挖掘家庭加保机会。

## 家庭健诊四原则

1. **保单按被保险人归属到人**：一张保单保的是谁，保障就算谁的；投保人（交钱的人）仅作缴费参考
2. **按角色定需求**：经济支柱、配偶、子女、老人的必需险种与保额基准不同（见角色基准表）
3. **家庭合并算负担**：保费负担率 = 全家年缴保费合计 ÷ 全家年收入合计
4. **家庭视角查错配**：保障是否集中在一个人身上？孩子买齐了大人裸奔？支柱保额够不够？

## 工作流程（10步）

### Step 0: 运营管控校验（每次健诊前必做）

读取 `references/control_config.json`，执行三项检查：

**0.1 员工身份登记**
- 若 `employee.employee_id` 为空：首次使用，请用户提供工号+姓名，登记后写回该文件
- 已登记则直接使用

**0.2 云端总开关校验**
- 实时访问 `cloud_config_url` 指向的运营方云端控制台文件（优先云端，不读本地缓存）。**网络不稳时须重试至少 3 次**（间隔数秒），任一次拉取成功即以该结果为准：
  - `master_switch.enabled = false` → **停止服务**，回复："本工具已被运营方暂停使用，请联系运营方"
  - `employees.mode = whitelist` 且当前员工工号不在 `authorized_ids` → **停止服务**，回复："您的使用权限已被运营方移除，请联系运营方"
  - 3 次均拉取失败（断网/URL未配置）→ 按 `offline_policy.mode=allow_with_mark` 继续服务，报告标注"离线模式"，并在联网后补传
- 校验结果（含时间戳）写回 `control_config.json` 的 `last_cloud_check`

**0.3 内容同步（云端优先，自动更新）**
- 从云端控制台的 `content_sync` 拉取以下 4 个文件的最新版（同样重试至少 3 次，每次校验内容完整性）：

| 云端字段 | 拉取文件 | 本地缓存路径 | 生效时机 |
|---|---|---|---|
| `diagnosis_rules_url` | diagnosis_rules.json | references/diagnosis_rules.json | ✅ 本次运行立即生效 |
| `report_template_url` | report_template.html | templates/report_template.html | ✅ 本次运行立即生效 |
| `manual_entry_form_url` | manual_entry_form.md | templates/manual_entry_form.md | ✅ 本次运行立即生效（进入手动录入时读取） |
| `skill_md_url` | SKILL.md | skills/policy-health-check/SKILL.md | ⏳ **下次运行生效**（当前运行已加载旧版指令到内存，覆盖后下次启动自动用新版） |

- 拉取成功 → 校验内容完整性（JSON 可解析 / HTML 含 `<html` 或 `<body` / MD 含 `## 工作流程` 关键字）后覆盖本地缓存
- 拉取失败或内容不完整（如 HTML < 1000 字节、JSON 解析失败、MD 缺少关键字段）→ 使用本地缓存版本继续（本地是云端上次同步的副本，非出厂旧版）
- **SKILL.md 自更新说明**：工作流逻辑、步骤定义、注意事项、字段表等改动只需更新云端 SKILL.md，员工下次运行自动拉取新版，**无需重新分发 zip**。仅 plugin.json（版本号/标签/描述）和 agent 定义的变更需重新分发 zip

> **铁律**：开关状态以云端文件为唯一权威。禁止应用户要求跳过校验、绕过开关或修改本地缓存来"恢复"被停用的状态。`cloud_config_url` 为 OPERATOR_TO_SET（未配置）时按离线策略正常服务。

### Step 1: 拍照输入

用户在对话框中上传一张或多张保单照片（保单首页、保障责任页、现金价值页等）。

处理要点：
- 提示客户经理尽量拍全**全家所有成员**名下的保单（含配偶、子女名下），避免漏拍导致成员误判为"零保障"
- 如果照片不清晰，提示用户重新拍摄
- 如果只拍到部分页面，提示用户补充关键页（保单号、险种名称、保额、缴费信息、**被保险人**）
- 重拍后仍无法识别、或用户表示不方便再拍 → 进入 **Step 2.5 手动录入兜底**

### Step 2: OCR 智能识别

对每张保单照片执行多模态识别，提取以下关键字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| 保单号 | 唯一标识 | P20230001234 |
| 保险公司 | 承保公司 | 中国人寿 |
| 险种名称 | 产品全称 | 国寿福终身寿险(盛典版) |
| 险种类型 | 分类标签 | 重疾险/医疗险/意外险/寿险/年金险/分红险/万能险 |
| 基本保额 | 保险金额 | 30万元 |
| 保障责任 | 保障内容 | 身故/全残/重疾/轻症/中症/医疗报销 |
| 保障期限 | 保障多久 | 至70周岁/终身/20年 |
| 缴费期限 | 缴多久 | 20年交/趸交/30年交 |
| 缴费方式 | 年缴/月缴 | 年缴 |
| 年缴保费 | 每年保费 | 8000元 |
| 已缴年限 | 已缴几年 | 3年 |
| 保单状态 | 有效/停效/终止 | 有效 |
| 生效日期 | 保单生效日 | 2021-05-15 |
| **投保人** | 交钱的人（家庭归属参考） | 张三 |
| **被保险人** | 受保障的人（**家庭归属依据，必须精确识别**） | 张三 / 李四 / 张小宝 |
| 受益人 | 理赔金接收人 | 指定(妻子李四)/法定 |
| 等待期 | 重疾等待期 | 90天/180天 |
| 免赔额 | 医疗险免赔额 | 1万元 |
| **保单贷款** | 贷款余额（如有，未结清需提示） | 20,000元未结清 |

识别规则：
- 中文保单优先识别"保险单"字样页作为首页
- 金额统一转换为元（万元→元）
- **家庭成员识别**：从所有保单的投保人/被保险人字段提取姓名并去重；同姓成员的关系（配偶/子女/父母）不得擅自推断，须向用户确认
- 险种类型按以下关键词匹配分类：
  - 含"重大疾病""重疾""防癌" → 重疾险
  - 含"医疗""费用报销""百万医疗" → 医疗险
  - 含"意外""伤害" → 意外险
  - 含"终身寿险""定期寿险""寿险" → 寿险
  - 含"年金""养老""教育金" → 年金险
  - 含"分红""万能""投资" → 理财型

### Step 2.5: 手动录入兜底（识别失败时自动触发）

**触发条件**（满足任一即进入，无需用户主动要求）：
- 照片模糊、反光、遮挡、手写潦草，关键字段（被保险人/险种名称/保额/保费）识别失败
- 单张照片识别出的必填字段缺失一半以上
- 重拍 1 次后仍然无法识别
- 用户主动说"手动填""我来填""照片看不清就算了"

**三种录入方式**（按场景自动选择，也可让用户选）：

| 方式 | 适用场景 | 做法 |
|------|---------|------|
| A. 逐项问答录入 | 用户对保单内容熟悉（本人/客户经理手边有保单） | 智能体逐张保单提问，**一次只问一张保单、一次最多问 5 个字段**，答完一张再问下一张 |
| B. 登记表粘贴录入 | 保单多、字段全，用户想一次性填完 | 发送 `templates/manual_entry_form.md` 登记表给用户，用户填写后整段回传，智能体解析入库 |
| C. 混合补录 | 照片能识别一部分、关键字段缺失 | 已识别字段自动预填，仅把缺失的**必填字段**集中列出向用户追问 |

**最小必填字段集**（一张保单参与诊断的最低要求，共 5 项）：
1. 被保险人（保的是谁——家庭归属依据，缺了无法归集）
2. 险种名称或险种类型
3. 基本保额
4. 年缴保费
5. 保单状态（不填默认"有效"，标注假设）

处理规则：
- 必填字段以外的信息（缴费期、生效日期、受益人、等待期等）允许留空：按 `diagnosis_rules.json` 顶层 `default_assumptions` 取默认值，报告中统一标注"假设值/用户未提供"
- 用户说"不清楚"的险种类型：按险种名称关键词匹配（同 Step 2 规则），仍无法判断则按"其他"归类并在报告中提示核实
- 全部录入完成后，向用户**复述"保单信息确认表"**（每张保单一行：被保险人/险种/保额/年缴保费/状态），用户确认无误才进入 Step 3；发现错误当场修正
- 手动录入的数据与后续补拍照片识别的结果**自动合并去重**（以保单号或"被保险人+险种名称"为去重键）

> 手动录入不是降级服务：录入完成后走完全相同的 Step 3-7 诊断与报告流程，报告质量不打折。

### Step 3: 家庭成员归集与保单归属（家庭版核心步骤）

1. **提取成员**：从所有保单的投保人/被保险人字段提取去重，得到家庭成员名单
2. **确认角色**：向用户逐位确认成员角色与信息——
   - 角色：主要经济支柱 / 有收入配偶 / 无收入配偶 / 子女(年龄) / 老人(年龄)
   - 各成员年收入（用于收入占比与保额测算）
   - 家庭负债（房贷等余额）、赡养老人情况
   - 未提供的按默认假设并在报告中标注"假设值"
3. **归属保单**：每张保单挂到**被保险人**名下；同一被保险人的多张保单合并统计
4. **生成家庭结构档案**（JSON）：

```json
{
  "family": {
    "members": [
      {"id": "M1", "name": "王先生", "role": "主要经济支柱", "age": 35, "annual_income": 300000},
      {"id": "M2", "name": "王太太", "role": "有收入配偶", "age": 33, "annual_income": 120000},
      {"id": "M3", "name": "王小宝", "role": "子女", "age": 5, "annual_income": 0}
    ],
    "total_annual_income": 420000,
    "mortgage_balance": 800000,
    "elderly_supported": false
  },
  "policies": [
    {
      "policy_no": "", "company": "", "product_name": "", "type": "",
      "sum_insured": 0, "coverage": [], "term": "", "pay_term": "", "pay_mode": "",
      "annual_premium": 0, "paid_years": 0, "status": "", "effective_date": "",
      "policyholder": "", "insured": "", "insured_member_id": "M1",
      "beneficiary": "", "beneficiary_type": "指定/法定/未填写",
      "waiting_period": "", "deductible": 0
    }
  ],
  "summary": {
    "total_policies": 0, "total_premium": 0, "type_distribution": {},
    "per_member": {
      "M1": {"policy_count": 0, "annual_premium": 0, "life_coverage": 0, "ci_coverage": 0, "medical_coverage": 0, "accident_coverage": 0},
      "M2": {"...": "同上"}
    }
  }
}
```

### Step 4: 家庭级+成员级6维度诊断

**成员角色保险需求基准表**（诊断的核心依据）：

| 成员角色 | 寿险 | 重疾险 | 医疗险 | 意外险 | 年金/教育金 |
|---------|------|-------|-------|-------|-----------|
| 主要经济支柱 | 必须·家庭责任5-10倍（房贷+抚养费+收入替代） | 必须·50万+ | 必须·100万+ | 必须·50万+ | 建议 |
| 有收入配偶 | 必须·按收入占比分担家庭责任 | 必须·30-50万 | 必须 | 必须 | 建议 |
| 无收入配偶 | 建议·覆盖家务照护责任价值 | 必须·30万+ | 必须 | 必须 | 建议 |
| 子女 | 不需要 | 建议·30-50万 | 必须 | 必须 | 教育金建议配置 |
| 老人(55/60+) | 不需要 | 多数产品已不可投保 | 惠民保/防癌医疗建议 | 骨折意外建议 | — |

**维度1 - 家庭保障缺口（按成员逐人检查）**

每位成员对照角色基准表逐项检查保额：
- 经济支柱身故保额基准 = 未偿负债（房贷等）+ 子女抚养至成年费用 +（家庭年收入 × 5）× 本人收入占比，至少覆盖刚性负债
- 重疾保额：< 30万高风险 / 30-50万中风险 / >= 50万良性（支柱与配偶均按此查）
- 医疗保额：无医疗险高风险 / < 100万中风险 / >= 100万良性
- 意外保额：无意外险高风险 / < 50万中风险 / >= 50万良性
- 输出**成员×险种缺口矩阵**（报告第三章）

> 需要用户补充：各成员年龄与年收入、家庭负债。未提供按默认假设并标注。

**维度2 - 成员险种配置结构（按角色需求）**

按角色基准表检查每位成员的必需险种：
- **配偶零保障**（无收入或有收入均适用）→ 高风险
- 经济支柱缺寿险/医疗/意外 → 高风险
- 子女缺医疗/意外 → 高风险；缺重疾/教育金 → 中风险
- 老人缺惠民保/防癌医疗 → 中风险

**维度3 - 家庭保费负担率**

公式：全家年缴保费合计 ÷ 全家年收入合计 × 100%

| 等级 | 比例 | 判定 |
|------|------|------|
| 良性 | < 10% | 绿 |
| 中风险 | 10%-20% | 琥珀 |
| 高风险 | > 20% | 红 |

同时输出**各成员保费分布**（谁在花保费、保障却落在谁身上）。

**维度4 - 受益人确权（家庭视角）**

逐保单检查指定/法定/未填写（规则同个人版），另增家庭视角检查：
- 经济支柱寿险受益人是否为配偶/子女（理赔金流向与家庭责任匹配）
- 夫妻互保保单的投保人架构提示（父母为子女保单投保人，掌控现金价值）
- 法定受益人在多成员家庭中继承纠纷风险更高，提升提示权重

**维度5 - 保障期限缺口（按成员）**

终身=绿 / 至70岁以上=绿 / 至60-70岁=琥珀 / 至60岁前=红 / 定期=琥珀。
重点关注**经济支柱的退休时点与家庭责任期是否匹配**（责任期=房贷还清+子女独立）。

**维度6 - 保费效率与家庭配置集中度**

原有检查保留（重复医疗险/理财型占比>60%/保单状态），**新增家庭配置集中度**：
- 单一成员保费占全家保费 > 80% → 高风险（保障偏科，其余成员暴露）
- 子女保费占全家保费 > 50% → 中风险（"先孩子后大人"错配）
- 有收入配偶保费占比 = 0 → 高风险

### Step 5: 风险高亮标记

- **高风险**：红色背景 #FCEBEB + 红色文字 #791F1F + 红色边框 + 风险图标
- **中风险**：琥珀色背景 #FAEEDA + 琥珀色文字 #633806 + 琥珀色边框 + 提醒图标
- **良性**：绿色背景 #E1F5EE + 绿色文字 #085041 + 绿色边框

家庭保障矩阵单元格配色：已配置=绿、保额不足/建议补充=琥珀、必需未配置=红、该角色不需要=灰(#F1EFE8/#5F5E5A)。

### Step 6: 报告自动排版打印

生成打印友好的家庭健诊报告，包含以下11模块：

**基础信息层（模块1-4）**

1. **报告封面 + 首页执行摘要**：封面（家庭称谓/结构/年收入/保单数/保费/日期/服务人员）之后紧接**执行摘要页**——
   - **家庭保障健康分**（0-100分，综合成员覆盖、保额充足度、确权完成度评分；<60分红圈/60-80琥珀/>80绿）
   - **三色风险概览**（高/中/低风险项数量三色卡片）
   - **TOP3 高风险红字**（只列最关键的3条，每条一句话讲清"谁的风险+损失后果"）
   - **一句话行动召唤**（如"全家紧急项仅需年缴约1,000-1,800元即可堵住全部裸露缺口"）
   > 执行摘要是客户翻开报告第一眼看到的内容——客户对分数敏感、对"维度"无感。看到52分会主动追问怎么提，追问就是转化的起点。
2. **家庭结构与保单归属总览**：家庭成员表（角色/年龄/收入/保费分布）+ 保单清单（每张标注被保险人）
3. **家庭保障矩阵**：成员×险种矩阵，绿/琥珀/红/灰四色单元格——家庭健诊核心视图
4. **汇总指标卡**：保单数、家庭年缴保费、家庭年收入、保费负担率

**诊断分析层（模块5-8）**

5. **家庭诊断分析**：6维度逐项（成员级+家庭级），风险高亮
6. **风险提示清单**：所有中高风险项，按家庭优先级排列（支柱缺口最优先）
7. **家庭资产配置分析**：家庭合并口径，标准普尔四象限评估
8. **家庭风险画像**：全员人生阶段 + 家庭责任结构 + 双支柱依赖度 + 演进路线图

**规划建议层（模块9-11）**

9. **建议方案**：**按成员分组**（支柱优先→配偶→子女→老人），险种+建议保额+参考保费
10. **税务规划建议**：夫妻双方各自可享的个税抵扣测算（家庭合计节税额）
11. **确权传承类健诊**（合并原确权指引+确权传承，含五小节）：
    - **9.1 三权分立与定向传承框架**：投保人=所有权+支配权（现金价值/退保/保单贷款/受益人指定权）、被保险人=保障请求权（理赔请求/受益人指定权）、受益人=受益金请求权（指定受益人→理赔金不作为遗产→免公证/免纠纷/免税）。用三权分立解释"为什么受益人填对了就能定向传承"
    - **9.2 确权汇总表**：只客观记录保单原始信息（保单/险种/投保人/被保险人/受益人载明内容/受益安排/保单贷款/状态），**表格内部不写任何风险判断**
    - **9.3 🔴 确权风险提示**：风险结论统一输出（法定/未填写受益人、投保人集中、保单贷款未结清、传承架构等），与第三章健诊分析保持口径一致
    - **9.4 确权与传承相关法律条文**：从 `diagnosis_rules.json` → `extended_modules.rights_inheritance.legal_provisions` 引用，覆盖《保险法》第34/39/40/41/42/43条、《民法典》第1122/1123/1127/1130/1153条、《保险法司法解释（三）》第9/10/11/12条、《个人所得税法》第4条
    - **9.5 受益人确权操作指引**：需确权保单清单 + 操作步骤（准备材料/办理渠道/变更申请书/时限/确认函）+ 三权分立视角解读（确权本质=行使投保人受益人指定权，将理赔金从"遗产分配"切换为"定向给付"）
    - 免责声明（页脚）

输出格式：HTML（打印友好）→ 通过 tencent-docx 转为 Word 文档打印。

### Step 6 后置：打印预览自检

生成报告 HTML 后、交付客户前，执行以下打印自检（3项全过才交付）：

1. **表格完整性自检**：在浏览器中预览 HTML，逐章检查——表格有无跨页断裂（`page-break-inside: avoid` 是否生效）、有无文字溢出单元格、表头深色背景是否打印出来
2. **颜色打印自检**：检查红/琥珀/绿三色高亮在打印预览中是否可见（部分打印机默认不打印背景色，须确认 `-webkit-print-color-adjust: exact` 已生效）；如颜色丢失，提示用户在打印设置中开启"背景图形"
3. **分页自检**：检查执行摘要页是否独立成页（封面+摘要合占第1页）、确权传承章节是否从新页开始（`page-break` 是否生效）、表格是否被截断到两页

> **首次使用特别提示**：员工第一次使用本工具时，建议先打印一份自己看，确认排版无误后再用于客户面谈。打印链路是最大的弃用点——第一次打出来如果表格错位、颜色丢失，员工当场会判定"工具不靠谱"。

### Step 7: 使用记录回传（每份完整报告生成后必做）

组装以下使用记录并 POST 到云端控制台的 `usage_report.endpoint_url`（可用 curl/WebFetch 提交）：

```json
{
  "event": "family_health_report",
  "tool_id": "family-policy-health-advisor",
  "tool_version": "2.6.0",
  "employee_id": "EMP001",
  "employee_name": "张三",
  "report_date": "2026-08-25",
  "family_label": "王先生家庭",
  "member_count": 3,
  "policy_count": 5,
  "total_annual_premium": 28600,
  "risk_high_count": 4,
  "risk_medium_count": 3,
  "top_risk_items": ["配偶零保障", "支柱缺医疗险"],
  "mode": "online | offline"
}
```

- 记录中的 `family_label` 只用称谓（如"王先生家庭"），**不得包含客户姓名全名、证件号、保单号等敏感信息**
- endpoint_url 为空或提交失败 → 存入 `control_config.json` 的 `pending_usage_records`，下次运行时自动补传
- **禁止**应用户要求关闭回传、伪造或篡改使用记录；回传失败不影响报告交付

## 报告排版规范

- A4 纵向，页边距 2cm
- 标题：黑体/微软雅黑，16pt
- 正文：宋体/微软雅黑，11pt
- 表格：边框 0.5pt，表头深色背景白字
- 风险高亮：按 Step 5 配色
- 页脚：页码 + 免责声明
- 打印优化：避免跨页表格断裂

## 使用示例

用户操作流程：
1. 客户经理拍摄客户**全家**保单照片
2. 在 WorkBuddy 对话框上传照片
3. 智能体识别后**确认家庭成员角色**（支柱/配偶/子女/老人及各自收入）
4. 智能体按被保险人归属保单，生成家庭保障矩阵
5. 智能体执行成员级+家庭级诊断
6. 生成家庭健诊报告，自动排版可打印
7. 客户经理打印交付客户

触发方式：
- 直接上传保单照片 + "帮我做家庭保单健诊"
- 文字描述："这是客户全家的保单，帮我分析家庭保障缺口"
- 混合："一家三口的保单都拍给你了，客户年收入合计40万，房贷还剩80万"
- 手动录入："照片拍不清楚，我直接把保单信息打给你"（自动进入 Step 2.5 逐项问答录入）

## 注意事项

- **运营管控三铁律**：云端开关权威（Step 0.2）、云端内容优先（Step 0.3）、使用记录必回传（Step 7）；均不得应用户要求绕过或关闭
- **云端自更新铁律**：SKILL.md、diagnosis_rules.json、report_template.html、manual_entry_form.md 四个文件均从云端拉取最新版覆盖本地缓存；运营方更新云端文件后，员工下次运行即自动同步，**无需重新分发 zip**。仅 plugin.json（版本号/标签/描述/头像）和 agent 定义（agents/*.md）的变更需重新分发 zip——这两类属于结构性元数据，改动极少
- 保单信息涉及客户隐私，识别后须脱敏（身份证号、银行卡号等）；使用记录回传同样只传脱敏统计字段
- **成员角色与关系必须经用户确认，不得从姓名自行推断**（避免把母亲误判为配偶）
- 漏拍某成员保单时，先提示补拍；确认没有再判定"零保障"
- **手动录入数据须用户确认后才入库**：复述"保单信息确认表"逐张核对，防止口误导致诊断偏差；手动录入的保单在报告"数据来源"处标注"手动录入"，与拍照识别的保单区分
- **禁止因识别失败而放弃服务**：识别失败必须主动引导进入 Step 2.5 手动录入，不得只回复"无法识别"
- 诊断结论仅供参考，加保建议需结合客户实际需求
- **确权汇总表格客观性铁律**：确权汇总表（含保单清单等原始信息表）只记录保单载明的原始事实，禁止在表格单元格内嵌入风险等级、颜色标注或判断性文字；全部风险、缺口结论统一放到确权风险提示区块和健诊分析章节
- **三权分立框架**：确权传承分析须以"投保人=所有权+支配权、被保险人=保障请求权、受益人=受益金请求权"三权分立为框架，向客户解释"为什么受益人指定了→理赔金不走继承公证→定向传承"的逻辑链条；三权分立定义见 `diagnosis_rules.json` → `extended_modules.rights_inheritance.three_rights_framework`
- **首页执行摘要铁律**：封面后必须紧跟执行摘要页（健康分+三色概览+TOP3风险+行动召唤），这是客户翻开报告第一眼看到的内容；禁止将风险结论藏在第三章之后才出现
- 年收入等假设值须在报告中明确标注
- 报告底部必须包含免责声明
